# Fidelity ledger

Track status and evidence separately for rules, AI, controls, persistence, layout,
graphics, animation timing, text, audio, music, video, error behavior, and packaging.
Use `unknown`, `documented`, `observed`, `implemented`, and `validated` rather than one
misleading completion percentage. Link every validated claim to a reproducible test or
captured observation.
