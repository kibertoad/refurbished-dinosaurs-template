# Rules and evidence

Assign stable identifiers such as `RULE-COMBAT-001`. For each rule record the player-
visible behavior, preconditions, exact resolution, edge cases, evidence sources,
confidence, implementation entry point, automated tests, and remaining uncertainty.
Distinguish manual statements, runtime observations, data-file facts, and static binary
evidence. When sources conflict, preserve the conflict instead of silently choosing one.
