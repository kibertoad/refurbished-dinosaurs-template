# UI atlas and interaction map

For every original screen or composite sheet, record native dimensions, palette,
regions, layers, transparency, animation frames, draw order, and scaling. Define control
hit rectangles separately from artwork and map each to keyboard, mouse, and controller
actions. Record hover/pressed/disabled states and route transitions. Keep original
screenshots outside Git; use coordinates and concise factual diagrams here.
