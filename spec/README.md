# {{ORIGINAL_TITLE}} specification

## Scope

Replace this paragraph with what the spec covers: the original game, the builds
it describes (see `builds/`), and the parts of the game that are in and out of
scope. The spec describes the original game and nothing else. It never names a
class, file or setting from the rebuild in this repository.

## Standard version

This spec follows version 1 of the
[documentation standard](https://dinorefurb.com/documentation-standard/).

## Areas

Areas are added to this list and never removed or renamed. `RNG` and `SAVE`
are common starting points. Delete a row only while no entry uses it.

| Area | Covers |
|---|---|
| `RNG` | Random number generators and the functions that reduce their draws to ranges. |
| `SAVE` | Save files and the state they hold. |
